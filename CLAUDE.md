# CLAUDE.md — スペック駆動開発 (SDD) ガイド

## 開発哲学

このプロジェクトは **スペック駆動開発 (Spec-Driven Development / SDD)** で進める。
コードを書く前に「何を作るか」を仕様書として定義し、人間と AI が合意してから実装に入る。

> 絶対に守るルール: **スペックなしに実装しない。**

これは新機能・仕様変更など「何を作るか」から考える必要がある作業に適用する。
下記「SDD対象外の作業」に該当する保守タスクは、この限りではない。

---

## SDD対象外の作業（保守タスク）

依存関係の更新やセキュリティ修正のような保守作業に毎回フルSDD（要件→設計→タスク分解→人間承認）を通すのは
作業の重さに見合わない。以下に該当するものはスペック作成を省略し、直接ブランチを切ってPRを作成してよい。

**対象外（スペック不要・直接PR可）:**
- Dependabotによる依存関係のバージョン更新
- Dependabot alertsで報告されたセキュリティ脆弱性の修正
- 挙動を変えない範囲が明確な小さな修正（typo、lintエラー、フォーマット崩れなど）

**対象外の判断に迷ったら対象内（=フルSDDが必要）:**
- ユーザーに見える挙動・UIが変わるもの
- 既存の仕様書（`.claude/specs/`配下）と矛盾する、または影響するもの
- 複数ファイル・複数コンポーネントにまたがる設計判断を伴うもの

**対象外の作業でも守ること:**
- PRの説明に「目的」「変更内容」「検証方法（実行したコマンドと結果）」を書く
- CIが通ること（`cargo fmt` / `cargo clippy` / `cargo check` / `cargo test`、フロントエンドは`vue-tsc`）
- マージの可否は、変更の種類ごとに定めた運用ルール（自動化ループの設定を参照）に従う。迷ったら人間の判断を仰ぐ

---

## ディレクトリ構造

```
プロジェクトルート/
├── CLAUDE.md                  # このファイル（SDD ルール）
├── .claude/
│   ├── specs/                 # 機能仕様（フェーズごとに管理）
│   │   └── {feature-name}/
│   │       ├── 01_requirements.md   # 要件定義
│   │       ├── 02_design.md         # 設計
│   │       ├── 03_tasks.md          # タスク分解
│   │       └── 04_progress.md       # 実装進捗
│   └── constitution.md        # プロジェクト憲法（技術スタック・規約）
└── src/                       # 実装コード（スペックから生成）
```

---

## SDD フェーズ定義

### Phase 1 — 要件定義 (Requirements)

**目的**: 「何を作るか」を明確にする。  
**成果物**: `.claude/specs/{feature}/01_requirements.md`

含む内容:
- ユーザーストーリー (`As a ... I want ... So that ...`)
- 受け入れ条件 (Acceptance Criteria)
- スコープ外の明示

**→ 人間がレビューして承認するまで次フェーズに進まない。**

---

### Phase 2 — 設計 (Design)

**目的**: 「どう作るか」のアーキテクチャを決める。  
**成果物**: `.claude/specs/{feature}/02_design.md`

含む内容:
- コンポーネント設計 / データ構造
- API インターフェース
- 技術的制約・判断理由

**→ 人間がレビューして承認するまで次フェーズに進まない。**

---

### Phase 3 — タスク分解 (Tasks)

**目的**: 実装を小さく独立したタスクに分割する。  
**成果物**: `.claude/specs/{feature}/03_tasks.md`

フォーマット:
```markdown
- [ ] Task 1: ○○を実装する
- [ ] Task 2: ○○のテストを書く
- [ ] Task 3: ○○と結合する
```

**→ 人間がレビューして承認するまで実装しない。**

---

### Phase 4 — 実装 (Implementation)

**目的**: タスクリストに従い、1 タスクずつ実装する。  
**ルール**:
- 1 タスクを完了したら `03_tasks.md` のチェックボックスを更新する
- タスクの範囲を超える変更はしない
- 仕様と矛盾する実装が必要な場合は実装を止め、先にスペックを修正する

---

## Claude への指示ルール

### スペック作成時

```
「{機能名} の要件定義を作成してください。.claude/specs/{feature}/01_requirements.md に保存してください。」
```

### 設計作成時

```
「01_requirements.md をもとに設計書を作成してください。.claude/specs/{feature}/02_design.md に保存してください。」
```

### タスク分解時

```
「02_design.md をもとにタスクリストを作成してください。.claude/specs/{feature}/03_tasks.md に保存してください。」
```

### 実装時

```
「03_tasks.md の Task 1 を実装してください。タスクの範囲のみ変更してください。」
```

---

## 禁止事項

- スペックなしに「とりあえず実装」しない
- レビュー承認前に次フェーズに進まない
- タスクに記載のない変更を実装中にこっそり加えない
- スペックと矛盾するコードを黙って書かない

---

## プロジェクト憲法の参照

技術スタック・コーディング規約・命名規則は `.claude/constitution.md` を参照すること。
憲法に記載がない判断が必要な場合は、人間に確認してから進める。
